<g>
  <!-- HACK: group needed to avoid 'multiple directive resource contention' exception -->
  <circle
    class="anchor"
    bp-selectable="data"
    selection-type="anchor"
    ng-attr-cx="{{data.position[0]}}"
    ng-attr-cy="{{data.position[1]}}"  
    r="10"
  />
</g>